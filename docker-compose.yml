services:
  mesh-assistant:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mesh-assistant
    ports:
      - "127.0.0.1:8787:8787"
    env_file:
      - .env
    volumes:
      - telegram-data:/app/.telegram-data
    restart: unless-stopped

volumes:
  telegram-data:
